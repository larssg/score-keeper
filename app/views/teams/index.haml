- title 'Teams'[]

- cache(teams_path) do
  %table.list
    %tr
      %th= 'Members'[]
      %th= 'Total ranking'[]
      %th= 'Games played'[]
      %th= 'Games won'[]
      %th= 'Winning percentage'[]

    - @teams.each do |team|
      %tr{ :class => cycle('odd', 'even') }
        %td= team[:players].collect(&:full_name).join(' - ')
        %td= team[:total_ranking]
        %td= team[:games_played]
        %td= team[:games_won]
        %td= team[:percentage].to_s + '%'