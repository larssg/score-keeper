- title 'Teams'[], :show => true

- cache(cache_key) do
  .list
    %table.teams
      %tr
        %th{ :colspan => 5 }= 'Members'[]
        %th.ranking= 'Total ranking'[]
        %th= 'Matches won'[]
        %th= 'Matches played'[]
        %th= 'Winning percentage'[]
        %th{ :colspan => 2 }= '% of matches on team'[]

      - @teams.each do |team|
        %tr{ :class => cycle('odd', 'even') }
          %td.user= user_link_full(team[:players].first)
          %td.user -
          %td.user= user_link_full(team[:players].last)
          %td.user
            = link_to image_tag('icons/game.png', :class => 'team', :alt => "Show this team's matches"[]), matches_path(:filter => team[:team_ids])
          %td.user
            = link_to image_tag('icons/chart_bar.png', :class => 'team'), team_path(team[:team_ids])
          %td.ranking= team[:total_ranking]
          %td= team[:matches_won]
          %td= team[:matches_played]
          %td= team[:percentage].to_s + '%'
          - team[:players].each do |player|
            %td
              = (((team[:matches_played].to_f / player.memberships_count.to_f) * 1000).to_i / 10.0).to_s + '%'