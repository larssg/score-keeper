- title 'Listing User OpenIDs'

= link_to 'New OpenID for account'[], new_user_user_openid_url
= ' | '
= link_to 'Back'[], edit_user_url(@user)

%table
  %tr
  - @user_openids.each do |user_openid|
    %tr
      %td= link_to user_openid.openid_url, user_openid.openid_url
      %td= link_to 'Destroy'[], user_user_openid_path(@user, user_openid), :confirm => 'Are you sure?'[], :method => :delete if current_user == @user

%br

= link_to 'New OpenID for account'[], new_user_user_openid_url
= ' | '
= link_to 'Back'[], edit_user_url(@user)
